
  // onRaceChange = e => {
  //   this.props.setRace(e.target.value);
  //   this.updateAbility(
  //     race.find(v => { v.name === e.target.value }).ability_bonus
  //   );
  // }

  // onSubRaceChange = e => {
  //   this.props.setRace(e.target.value);
  //   this.updateAbility(
  //     race.find(v => v.name === e.target.value)
  //       .find(v => v.sub_races.name === e.target.value).ability_bonus
  //   );
  // }

  // updateAbility(bonus) {
  //   this.props.setAbility(
  //     this.props.dice.map((v, k) => {
  //       return v + bonus[k];
  //     })
  //   );
  // }